
describe('Prendi Assessment', () => {
  describe('canvas testing 1', () => {
    //In order to test this canvas I need to create a mockedCanvas class first
    //that could hold the values after interactions with it
    //so moves can be emaulated and finally the returned items can be asserted.
    
    test('should assert a move from a specific coordenates', () => {
      expect(true).toEqual(true);
    });
  });

});




